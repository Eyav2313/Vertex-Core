# Vertex Glass Assets

The lock/login theme uses the same visual direction as `preview/desktop/index.html`.

Primary assets:

```text
vertex-logo.png
vertex-lock-background.png
```

Target size: 3840x2160 PNG. The image should be calm, high contrast behind glass surfaces, and avoid noisy detail near the center login panel.
