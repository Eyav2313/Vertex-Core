[package]
name = "vertexctl"
version = "0.1.0"
edition = "2021"
description = "VertexOS system control helper"
license = "GPL-3.0-or-later"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
