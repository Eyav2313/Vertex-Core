# Build

Generated build logs and package outputs are written here, while heavy Linux build work is staged outside Windows-mounted folders when running under WSL.

Important subfolders:

- `profiles/`: package manifests for each image profile.
- `logs/`: master build logs.
- `packages/`: local Debian package outputs.
